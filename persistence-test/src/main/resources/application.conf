akka {
  persistence {
    journal {
      plugin = "akka-persistence-redis.journal"
    }
    snapshot-store{
      plugin = "akka-persistence-redis.snapshot"
    }
  }
}

redis {
  host = "localhost"
  port = 6379 # redis 기본 포트번호
}